<?xml version="1.0" encoding="utf-8"?>
<!--
  这台机器连不上 nuget.org（公司网络），但每次 restore 仍会挨个源做 DNS/TCP 超时等待：
  实测一次 restore 要 135 秒，而真正的编译打包只要 6 秒——也就是说 96% 的发布时间花在
  等一个连不通的地址上，附带每个项目刷一条 NU1900「Unable to load the service index」警告。

  本项目的包（OxyPlot.Wpf / PdfPig / Microsoft.Data.Sqlite 等）早就躺在本地 global-packages
  缓存里，不需要联网。<clear/> 掉所有源之后 restore 直接命中缓存，秒级完成，警告也一并消失。

  ⚠ 要新增/升级 NuGet 包时：把这个文件临时改名，在能上网的环境跑一次 restore 把包灌进缓存，
  再改回来。否则会报 NU1101「找不到包」——那是预期行为，不是配置坏了。
-->
<configuration>
  <packageSources>
    <clear />
    <add key="nuget.org" value="https://api.nuget.org/v3/index.json" />
  </packageSources>
</configuration>
