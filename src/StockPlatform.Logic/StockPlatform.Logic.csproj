<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>StockPlatform.Logic</RootNamespace>
    <LangVersion>latest</LangVersion>
    <DebugType>none</DebugType>
  </PropertyGroup>

  <ItemGroup>
    <!-- 指数全集清单（IndexCatalog 读取），随程序打包，不运行时依赖聚宽网页 -->
    <EmbeddedResource Include="Services\IndexCatalog.csv" />
  </ItemGroup>

</Project>
