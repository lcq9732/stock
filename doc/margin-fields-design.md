# 两融字段补全与融券余额补算

> 状态：**全部完成**（2026-09-17）。取价口径已改成不复权 `day_raw`，全历史重算已跑完，
> 并用深市官方值做了跨年份正确性验证（见 §9）。
> 起因：用户问"融资净买入额这个数据能有吗"，查证时发现三个字段一直没解析、以及沪市融券余额恒为 0。

---

## 1. 查出来的两个问题

### 1.1 三个字段数据源一直在返回，我们没解析

实测 2026-09-14 的真实响应：

**上交所** `query.sse.com.cn/.../queryMargin.do`（JSON）实际返回

```json
{"stockCode":"510050","securityAbbr":"50ETF",
 "rzye":1481494528,      // 融资余额     ✓ 已取
 "rzmre":88951106,       // 融资买入额   ✓ 已取
 "rzche":59724213,       // 融资偿还额   ✗ 没取
 "rqyl":36564540,        // 融券余量     ✓ 已取
 "rqmcl":1415000,        // 融券卖出量   ✗ 没取
 "rqchl":284500,         // 融券偿还量   ✗ 没取
 "rqylje":null}          // 融券余额     ← 恒为 null，见 1.2
```

**深交所** `szse.cn/api/report/ShowReport`（xlsx）共 8 列，原先只读 0,1,2,3,5,6：

| 列 | 含义 | 原先 |
|---|---|---|
| 0 / 1 | 代码 / 简称 | ✓ |
| 2 / 3 | 融资买入额 / 融资余额 | ✓ |
| **4** | **融券卖出量** | **跳过了** |
| 5 / 6 | 融券余量 / 融券余额 | ✓ |
| 7 | 融资融券余额（= 3+6） | 不取，可算 |

深交所**没有**融资偿还额、融券偿还量这两项。

跟东财 `columns=ALL` 那次是同一个毛病（project_em_columns_all_dropped）：**缺字段是解析丢的，不是没取到，加字段零新增请求。**

### 1.2 沪市融券余额恒为 0 —— 静默失效了很久

上交所的 `rqylje` **恒为 null**，而解析用的 `GetNum` 把 null 读成 0：

```
沪市 MarginDetail 行数            3,139,554
其中 short_balance > 0 的行数             0     ← 历史上从来没有过非 0 值
深市 short_balance > 0 的行数     （当日 2430 只里 69% 有值，合计 106.5 亿）
```

后果是静默的：资金面诊断的「融券余额变化」对**所有沪市票**算不出来，而界面上显示的是
"融券余额 0"——看起来像"这只票没人做空"。宁德时代是创业板（深市），所以做那个功能时没暴露。

**教训**：凡是源头可能不给的列，模型里就得可空。把"没有"写成 0，下游永远分不清
"这个市场不提供"和"当天确实是 0"。

---

## 2. 融券余额怎么补出来

公式是**交易所自己写的**。深交所报表页脚原文：

> 本日融资余额(元) = 前日融资余额 ＋ 本日融资买入 − 本日融资偿还额
> 本日融券余额(元) = **本日融券余量 × 本日收盘价**
> 本日融资融券余额(元) = 本日融资余额 ＋ 本日融券余额

沪市不给余额但给了余量，收盘价本地有 → 按同一公式补，跟深市完全同口径。

**实测验证**（2026-09-14）：

| 对照 | 样本 | 误差中位 | 90 分位 | 最大 |
|---|---|---|---|---|
| 深市（对交易所自己给的余额） | 1,603 只 | 0.0000% | 0.0000% | 0.709% |
| 沪市（对东财公布值） | 456 只 | 0.0000% | 0.0000% | 3.235% |

沪市那一版全部用**库里的数据**算（交易所给的余量 × 我们自己的收盘价），逐只对照东财：

| 代码 | 名称 | 库里余量 | 库里收盘 | 算出 | 东财公布 | 误差 |
|---|---|---|---|---|---|---|
| 600036 | 招商银行 | 9,771,516 | 41.83 | 408,742,514 | 408,742,514 | 0.000% |
| 601318 | 中国平安 | 5,374,850 | 54.73 | 294,165,540 | 294,165,540 | 0.000% |
| 600519 | 贵州茅台 | 127,848 | 1277.96 | 163,384,630 | 163,384,630 | 0.000% |

顺带确认**两个源的原始数据完全一致**：沪市 500 只票的融资余额逐一对照东财，
误差中位 0.000000%、最大 0.000000%、完全相同的占 100.0%。

---

## 3. 融资净买入：两种口径，结果等价

**净买入 = 买入额 − 偿还额。** 两条路都能算：

- **沪市**：源头给了 `rzche`，直接减
- **深市**：源头不给，用**融资余额的日变动**。依据就是上面那条页脚恒等式——
  余额差分 ≡ 买入 − 偿还

**实测同源验证**（贵州茅台，东财数据，余额差分 vs 官方公布的净买入）：

| 日期 | 余额差分 | 官方净买入 | 差 |
|---|---|---|---|
| 09-07 | 19,865,787 | 19,865,788 | −1 |
| 09-08 | 36,604,529 | 36,604,529 | 0 |
| 09-09 | 164,338,774 | 164,338,773 | +1 |
| 09-10 | −35,275,006 | −35,275,005 | −1 |
| 09-11 | −34,104,702 | −34,104,703 | +1 |
| 09-14 | −33,545,806 | −33,545,807 | +1 |
| **09-15** | −51,023,479 | −51,843,258 | **+819,779** |

±1 元是四舍五入。**只有最新一天对不上**——那天数据还没定稿（兆易创新同日差 580 万）。
跟「盘中K线不是最终值」是同一类问题（project_intraday_bar_confirmation）。

**所以不去抓偿还额的历史**：差分法已经等价、覆盖 2010-03-31 至今全历史，而抓偿还额
只能从今往后攒（两所都不提供历史查询，只能按交易日逐日抓）。新增的 `margin_repay` 列
老行一律 NULL，不回填。

### 3.1 买入额会骗人，必须和净买入并列

买入额是**总流水**（买了又还、还了又买都计在内）。宁德时代实测：

| 日期 | 融资买入额 | 净买入 | 当日涨跌 |
|---|---|---|---|
| 2026-08-06 | 17.0 亿 | **+7.45 亿** | −4.26% |
| 2026-09-08 | 15.9 亿 | **+2.53 亿** | −3.65% |
| 2026-08-27 | 11.1 亿 | **+6.00 亿** | −1.58% |

区间买入额合计 **231 亿**，净买入只有 **+12.25 亿**（19 倍差）。
9/8 买入额排区间第二，看着像大举抄底，实际净买入只有 2.53 亿——那天是**大进大出**。

所以维度4 的 Top3 表改成 `日期 | 融资买入 | 净买入 | 当日涨跌`、按净买入排序。
只给买入额会读成"猛加仓"，只给净买入又看不出流水规模，**两个必须并列**。

---

## 4. 实现

| 层 | 文件 | 做什么 |
|---|---|---|
| Schema | `SqliteSchema.cs` | `MarginDetail` 加 `margin_repay` / `short_sell_volume` / `short_repay_volume`；`CREATE TABLE` 和补列名单同时改 |
| 模型 | `MarginDetailRow.cs` | 新增三个 `double?`；`ShortBalance` 从 `double` 改成 `double?` |
| 解析 | `ExchangeMarginProvider.cs` | 沪市补 `rzche`/`rqmcl`/`rqchl`、`rqylje` 留 null；深市补第 4 列，偿还额两项留 null。新增 `GetNullableNum` |
| 写入 | `SqliteMarginRepository.cs` | 四个可空列写 `DBNull` 而不是 0 |
| 补算 | `SqliteMarginShortBalanceFiller.cs` | 按交易日分批 UPDATE |
| 任务 | `MarginShortBalanceFillTask.cs` + Catalog + App 装配 | 【融券余额补算】，本地查库·不联网 |
| 诊断 | `CapitalDiagnosisAnalyzer.cs` | 维度4 加净买入列（`NetBuySeries`）、融券余额可空处理 |
| 测试 | `MarginShortBalanceFillTests.cs` | 8 个，用临时库 |

### 4.1 补算器的五个要点

1. **只动沪市**。深市有 31% 的行 `short_balance = 0`，那是**真的 0**（当天确实没融券余量）。
   判据不能只看"是不是 0"，必须先分市场。
2. **市场判断走 `MarketClassifier`，ETF 走库里存的前缀**。`MarketClassifier` 的职责是
   "从 6 位**股票**代码判板块"，ETF 的 5xxxx 它返回 `Unknown`。而 ETF 的市场前缀在这个项目里
   **从来不是算出来的**——是新浪 ETF 列表接口直接给的 8 位符号（`sh510300`），原样存进
   `Bar`/`StockMeta`。所以 ETF 查这两张表里存的是 `sh` 还是 `sz`，**不另造"5 开头算上交所"的规则**
   （自写前缀规则的代价见 feedback_market_prefix_via_classifier）。
3. **ETF 找收盘价要试带前缀的代码**。两融表里是裸码 `510050`，`Bar` 里是 `sh510050`。
   只按裸码找会让 323 只本来有数据的 ETF 也算成"补不上"。
4. **按交易日分批提交**（沪市每天约 1700 行）。单事务写几百万行曾把 WAL 撑到 162 GB
   （project_wal_blowup_and_stale_dotnet）。分批还顺带解决了断点续——补过的行不再满足
   缺值判据，中断直接重跑。
5. ⭐ **取价走 `day_raw`（不复权），不走 `day`（前复权）**（2026-09-16 补，见下）。
   公式里的"当日收盘价"是当日**真实成交价**；`day` 是源给的减法式前复权，高分红老股的
   早年价格会被减成负数——贵州茅台 2012-05-02：`day` = **−127.19**、`day_raw` = 225.98。
   口径**逐标的**定（有 `day_raw` 就用，没有才回退 `day`）：ETF 眼下一只都没有 `day_raw`，
   一刀切的话要么个股陪着 ETF 用错口径、要么 ETF 整块补不出来。

### 4.2 回填规模

```
沪市有融券余量的行  2,697,619
能配到当日收盘价    2,631,346  (97.54%)   ← 会被补上
配不上               66,273  (2.46%)     ← 留 NULL
```

⚠ **这一段当时的归因是错的**，2026-09-16 逐项核查后推翻（详见
`doc/missing-instruments-design.md`）：

> ~~补不上的 2.46% 不是缺陷：478 个两融标的在库里一根日 K 都没有，构成是
> 465 只 ETF + 12 只历史退市/更名股票 + 1 只仍在交易的股票。~~

实际是：

| 当时的说法 | 实测 |
|---|---|
| 478 个标的一根日 K 都没有 | **15 个**（2 只已清盘 ETF + 12 只退市/换代码股 + 689009） |
| 其中 465 只 ETF 缺 K 线 | **2 只**。那 465 是"没有 `day_raw`"的标的数（463 + 15 = 478，数字就是这么来的），它们的 `day` 一直都在 |
| 科创板 588/589 缺 77 只 | **缺 0 只**——两融标的里的 77 只 588/589 全都有 K 线 |
| ETF 名单覆盖不全 | 名单是全的：`type='etf'` 1665 只，跟新浪 `etf_hq_fund` 全量（约 1600~1700）对得上 |

那 2.36%（当时算的 2.46%）**根本不是"缺 K 线"**，是**取价用了前复权**：
负价行被 `close > 0` 挡掉了。见 §8。

**689009 九号公司**那条是对的，根因也查实了：新浪 `hs_a` 节点不含科创板 CDR
（第 27 页从 sh688718 直接排到 sz000060），`node=kcb` 才有。已修（合并翻 kcb）。

---

## 5. 回填结果（2026-09-16 实跑）

用户手工发布后，在 Fetcher 里点【融券余额补算】跑了一次全历史：

```
15:14:52  开始，待补算 3999 个交易日（2010-03-31 ~ 2026-09-14）
16:02:45  完成：补上 2,741,280 行；仍缺 66,282 行
```

耗时 **48 分钟**。⚠ 我事先估"几分钟"是错的——那是按天数平均算的，没考虑行数分布：
两融标的从 2010 年的 90 只涨到现在 1700 只，头 1000 天只占总行数的 5.6%。
按天报进度时前期飞快、后期越来越慢，容易误判。

### 对账（全部通过）

| 检查项 | 结果 |
|---|---|
| 沪市 `short_balance > 0` | **0 → 2,631,346** 行 |
| 仍缺 | 66,273 行 —— 跟事先估的 66,273 **分毫不差** |
| **深市有没有被误改** | `short_volume>0 且 short_balance=0` 的行数 **0**，一行未动 |
| 库里值 vs 现场重算 `余量×收盘价` | 抽查 8 只，差 **0.00** |
| vs 东财公布值（沪市 09-14，499 只） | 误差中位 **0.0000%**，90 分位 0.0000%，99.6% 在 0.01% 以内 |
| **ETF**（512100/510500/560010/511380/510300） | 全部 **0.000%** —— 带前缀找收盘价那段是对的 |

### 两只偏差票：是东财那边的价格有问题

误差 ≥0.5% 的只有 2 只，查清了原因——**余量完全一致，差在收盘价**：

| 票 | 库里余量 | 东财余量 | 库里收盘 | 东财隐含价 | 余额差 |
|---|---|---|---|---|---|
| 600016 民生银行 | 36,002,420 | 36,002,420 | **3.59** | **3.71** | −3.23% |
| 600999 招商证券 | 346,900 | 346,900 | **17.20** | **17.37** | −0.98% |

我们的价格在前后几天里连贯（民生 09-11=3.58 → 09-14=3.59 → 09-15=3.59），东财那个 3.71
落在序列之外；两只票的比价也不一致（1.033 vs 1.010），排除统一的复权因子。
**判定东财那边算融券余额时用的价格异常，我们的更可信**，不跟。

---

## 6. 实机验证（2026-09-16）——抓出一个措辞 bug

按 feedback_verify_by_running_app，Debug 起 Analyzer（数据目录 junction 到真库）打开
**601318 中国平安**（沪市）的分析详情，读维度4 的实际输出。

**两件事验证通过**：

```
融券余额同期 -13.2%（3.4亿 → 2.9亿）—— 做空力量在撤出        ← 补算生效，沪市票有值了
区间融资买入额合计 25.1亿，净买入仅 2.5亿 —— 融资盘大进大出   ← 净买入列生效
本波融资净买入 Top3（买入额是总流水，净买入才是增仓）
  融资买入 | 净买入 | 当日涨跌
```

**同时抓出一个 bug**，界面上原本显示：

```
股价 -5.9%、融资余额 +0.5%，方向一致 —— 杠杆资金顺势减仓
```

两处都错：① 股价 −5.9% 和融资余额 **+0.5%** 符号明明**相反**，却说"方向一致"；
② "减仓"是拿**股价**方向判的（`px < 0 ? "减仓" : "加仓"`），而融资余额实际是**微增**。

根源是"变化 <1% 不报背离"那个阈值——它把这种情况甩进了 else 分支，而 else 的措辞
假定了"同向"。**单元测试漏掉它，是因为造的数据只有"明显背离"和"明显同向"两档，
没有中间这档**。现已改成三分支，并补了 2 个测试钉住：

```
股价 -5.9%，而融资余额几乎没动（+0.5%） —— 杠杆资金既没明显加仓也没撤出
```

这正是"单元测试测不出来、必须真跑程序"的实例：算法对、数据对、口径对，
错在**规则拼装出来的那句话跟数据不符**——而结论句恰恰是这个功能的主角。

---

## 7. 还没做的

**`margin_repay` / `short_sell_volume` / `short_repay_volume` 三列还全是 NULL**——
要等**下一次抓两融**才开始有值（新版 exe 已发布、解析已就位，历史不回填，理由见第 3 节）。
下次抓完查一次确认：

```sql
SELECT COUNT(*) FROM MarginDetail WHERE margin_repay IS NOT NULL;
```

有值之后，沪市的融资净买入就会自动改走官方口径（`买入 − 偿还`）而不是余额差分，
两者等价但官方口径不受"最新一天未定稿"影响。

---

## 8. ⚠ 取价口径错了，那 274 万行要重算（2026-09-16 晚）

### 8.1 错在哪

取价条件是 `granularity = 'day' AND close > 0`。`day` 是源给的**减法式前复权**，
而公式里的"当日收盘价"要的是当日**真实成交价**——也就是 `day_raw`。贵州茅台：

| 日期 | `day`（前复权） | `day_raw`（不复权） |
|---|---|---|
| 2012-05-02 | **−127.19** | 225.98 |
| 2018-06-29 | 457.66 | 731.46 |
| 2026-09-14 | 1277.96 | 1277.96 |

它 6005 根 `day` 里有 3529 根（59%）`close <= 0`；全库 `day` 有 745,340 行负价、
涉及 739 只标的，`day_raw` 一行都没有。

### 8.2 为什么 §5 的对账全过了却没发现

**对账只用了 2026-09-14 当天**。前复权以最新价为基准，最新那一天 `day == day_raw`，
误差天然是 0.0000%。错误全在历史行里，而且越往前越大：

| 票 | 日期 | 旧口径余额 | 新口径余额 | 偏差 |
|---|---|---|---|---|
| 600519 贵州茅台 | 2018-06-29 | 9,741 万 | 15,569 万 | **−37.4%** |
| 600036 招商银行 | 2018-06-29 | 3,397 万 | 6,930 万 | **−51.0%** |
| 601088 中国神华 | 2018-06-29 | 163 万 | 938 万 | **−82.6%** |
| 600519 贵州茅台 | 2013-06-28 | （负价被挡掉） | 13,225 万 | — |
| 任意票 | 2026-09-14 | = | = | **+0.0%** |

**"当天对得上"不能证明"历史对得上"**——这是这次最值得记住的一条。

### 8.3 改法与重算

取价**逐标的**优先 `day_raw`、没有才回退 `day`（`_sh_margin` 临时表加一列 `gran`）。
不一刀切的原因：ETF 眼下一只都没有 `day_raw`，沪市 2278 只两融标的里 1747 只有、
162 只只能回退。

预期效果（只读实测，尚未执行）：

```
沪市有融券余量的行  2,807,562
旧口径配不上          66,282  (2.36%)
新口径配不上          39,816  (1.42%)    ← 多补出 26,466 行
```

⚠ **直接重跑【融券余额补算】是没用的**：常规判据是"缺值才补"，已经填过的行不再满足判据，
错值会一直留着。所以 `Fill` 加了 `recompute` 参数——连已有值的沪市行也按当前口径重算。
重算是安全的：旧口径填得上的行新口径一定也填得上，不会留下"清成 NULL 又补不回来"的行；
深市依旧一行不碰。

**这次重算还没跑**（上次全历史跑了 48 分钟），等用户定怎么触发：
① 加一个【融券余额重算】按钮，② 还是一次性用脚本调一次就够了。

---

## 9. 重算与正确性验证（2026-09-17）

### 9.1 重算

任务层原先没把 `recompute` 传下去——Data 层改好了但界面点不到，等于没接上。补上之后按
**`FetchMode.Thorough`（彻底重查）** 触发，不是 `FirstBackfill`：项目 2026-09-11 定的词义分界是
「彻底重查＝不管原来有没有、全部重来一次；只补缺、不动已有的才是 FirstBackfill」，
这里要的正是前者。

重算结果：

| | 前复权那版 | 重算后 |
|---|---|---|
| 沪市 `short_balance > 0` | 2,631,346 | **2,660,704**（+2.9 万，原先被负价挡掉的补上了） |
| 仍缺（有余量无值） | 66,273 | **38,369**（−42%） |
| 深市 | 一行未动 | **一行未动** |

茅台的修正幅度，越往前越大（跟"前复权是减法式、累计分红越多减得越狠"吻合）：

| 日期 | 前复权（错） | 不复权（对） | 修正 |
|---|---|---|---|
| 2020-06-01 | 431,002,474 | 527,311,602 | +22.3% |
| 2021-06-01 | 2,529,759,917 | 2,836,348,006 | +12.1% |
| 2022-06-01 | 478,778,239 | 546,968,451 | +14.2% |
| 2026-06-01 | 140,599,579 | 143,673,597 | +2.2% |

### 9.2 正确性验证：拿深市官方值当基准

⚠ 先纠正一个方法错误：重算后我第一次做的验证是"库里值 vs 按同样公式重算"，
那是**自洽性检查不是正确性检查**——用同一个公式验自己当然全对。

真正的外部基准是**深市**：交易所直接给融券余额，我们从不计算、只存源头值。拿它验公式：

| 日期 | 样本 | `day_raw` 误差中位 | <0.01% 占比 | `day`（前复权）误差中位 | <0.01% 占比 |
|---|---|---|---|---|---|
| 2015-06-01 | 324 | **0.0000%** | 99.4% | 33.85% | 1.9% |
| 2018-06-01 | 376 | **0.0000%** | 100.0% | 15.79% | 5.1% |
| 2020-06-01 | 696 | **0.0000%** | 99.0% | 7.29% | 12.4% |
| 2022-06-01 | 863 | **0.0000%** | 100.0% | 4.63% | 14.5% |
| 2024-06-03 | 1498 | **0.0000%** | 100.0% | 2.80% | 17.4% |
| 2026-09-14 | 1603 | **0.0000%** | 100.0% | **0.0000%** | 99.3% |

逐只看（2018-06-01）：

```
002049 紫光国微   官方 87,052,505   raw 87,052,505  -0.00%   day 59,127,501  -32.08%
000858 五 粮 液   官方 67,404,621   raw 67,404,621   0.00%   day 41,547,036  -38.36%
000002 万  科Ａ   官方 37,902,800   raw 37,902,800   0.00%   day 29,360,092  -22.54%
```

这张表同时证明了三件事：公式对、`day_raw` 口径对、**以及我当初为什么没发现**——
最后一行 2026-09-14 两个口径都是 0.0000%，而我第一次抽样只取了最新一天。
**换任何一个历史日期，第一眼就会暴露。** 这是抽样设计的缺陷，不是运气。

### 9.3 还没做

**ETF 仍在用前复权**：它们目前没有 `day_raw`，回退用 `day`（加法式失真、约偏低 5%，不会为负）。
【ETF日K·不复权】跑完之后，再跑一次【融券余额补算·彻底重查】就能一并修正。
